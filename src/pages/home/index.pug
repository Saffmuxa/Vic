extends ../layout

append style
  link(rel='stylesheet' href=require('./home.scss'))

append script
  script(src=require('./main.js'))

block content

  main.main-content
    .wrapp
      h1.title Какая то игра!
      .title-form

        .btns
            button.js-btn-down.btn-home.dsbl(type="button") -1
            h4.output
            button.js-btn-up.btn-home(type="button") +1
        form(action='game.html').start-game.js-start-game
          .start-inputs
            .inputs
            input.main-menu_group1-name(type="text", name="name" placeholder='Введите название команды' required)
            //- input.main-menu_group1-name(name="name" placeholder='Введите название второй команды' required)
            input.main-menu_group1-timer(name="time" placeholder='Время на вопрос (сек)' type='number' required)
            button(type="submit").main-menu.js-submit Start Game
            
    .rules  
      h3 Немного подсказок:
      ol.tips(type="a")
        li При выборе вопроса - открывается наименование выбранной категории
        li После прочтения вопроса необходимо дать ответ как можно быстрее
        li Если команда отвечает неверно, очки отнимаются и дается возможность ответить другим командам 
        li Команды могут не отвечать после неверного ответа оппонента 
        li Активация кнопок "правильного" и "неправильного" ответов происходит после истечения времени, нажатию на Space или щелчка по самому таймеру
        
